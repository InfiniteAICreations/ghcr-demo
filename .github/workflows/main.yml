name: Build and publish multi-arch container images

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      reason:
        description: 'Why ?'
        required: false
        default: ''

jobs:
  ghr_push:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v3

      - name: Log-in to ghcr.io
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

#      - name: Read version from VERSION file
#        id: readv
#        uses: jaywcjlove/github-action-read-file@v2
#        with:
#          path: opendevin/sandbox/VERSION

      - name: Build and push multi-arch container images
        run: |
          # Find directories containing Dockerfile but not containing .ignoredockerfile
          while IFS= read -r dockerfile_dir; do
              # Check if .ignoredockerfile exists in the directory
              if [ ! -f "$dockerfile_dir/.ignoredockerfile" ]; then
                  # Change directory and execute 'make all'
                  pushd "$dockerfile_dir" > /dev/null
                  make all
                  popd > /dev/null
              fi
          done < <(find . -type f -name Dockerfile -exec dirname {} \; | sort -u)

#          # Find directories containing Dockerfile but not containing .ignoredockerfile
#          for dockerfile_dir in $(find . -type f -name Dockerfile -exec dirname {} \; | sort -u | grep -v "$(find . -type f -name .ignoredockerfile -exec dirname {} \; | sort -u)"); do
#              # Change directory and execute 'make all'
#              pushd $dockerfile_dir > /dev/null
#              make all
#              popd > /dev/null
#          done

#          for dir in evaluation/SWE-bench opendevin/sandbox; do
#            cd $dir
#            make all
#            cd -
#          done